using System;

namespace CodeSnippetsAndSpecialCapabilities
{
    /* Снипеты - специальные выражения среды разработки, которые используются для кодогенерации.
     * Для использования - введите название снипета и нажмите 2 раза Tab
     * 
     * Самые распространеные снипеты:
     * 1. class - создание класса
     * 2. enum - создание енумератора
     * 3. interface - создание интерфейса
     * 4. struct - создание структуры
     * 5. ctor - создание конструктора без параметров
     * 6. Свойства: 
     * prop (публичные get и set), 
     * propg (публичный get и приватный set), 
     * propfull (полное свойство)
     * 7. Циклы: for, foreach, while, do
     * 8. Условные операторы: if, else, switch
     * 9. Блоки try: try (try-catch), tryf (try-finally)
     * 10. using - блок using
     * 11. cw - создание Console.WriteLine()
    */

    // Обернуть снипетом - Ctrl + K , Ctrl + S
}
